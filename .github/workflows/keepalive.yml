name: Keep API alive
on:
  schedule:
    - cron: '0 8 * * *'  # daily at 8am UTC

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: curl -s https://pritesh2312.pythonanywhere.com/health
